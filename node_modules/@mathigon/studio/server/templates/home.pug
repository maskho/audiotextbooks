extends _layout

block vars
  - var title = config.siteName;

block main
  .container.narrow
    h1 #{config.siteName} Landing Page
    h3 Under Development
    //- for c in courses || []
    //-   +course(c)
    include _footer
