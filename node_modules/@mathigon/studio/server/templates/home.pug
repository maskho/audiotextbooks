extends _layout

block vars
  - var title = config.siteName;

block main
  .container.narrow
    h1 #{config.siteName} Landing Page (Coming Soon!)
    h3 You can sign in or create an account on the top right menu of this page.
    //- for c in courses || []
    //-   +course(c)
    include _footer
